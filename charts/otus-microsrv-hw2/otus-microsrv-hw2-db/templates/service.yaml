apiVersion: v1
kind: Service
metadata:
  name: {{ include "otus-microsrv-hw2-db.fullname" . }}
  labels:
    {{- include "otus-microsrv-hw2-db.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "otus-microsrv-hw2-db.selectorLabels" . | nindent 4 }}
